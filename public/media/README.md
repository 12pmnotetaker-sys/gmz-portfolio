# Local video

Walkthrough clips the portfolio plays from this repo rather than from Google
Drive. Referenced from a project's Markdown as `file: <name>.mp4`, and checked
at build time: name a file that is not here and `npm run build` stops.

See ASSETS.md for the list of clips still outstanding and how to wire one up.
